// Sanity CMS connection settings.
// After creating your Sanity project, copy cms-config.example.js to cms-config.js
// and replace the values below. The website will keep using its static content
// until a real projectId is configured and published CMS content exists.
window.SANITY_CONFIG = {
  projectId: "YOUR_SANITY_PROJECT_ID",
  dataset: "production",
  apiVersion: "2025-02-19",
  useCdn: true
};
